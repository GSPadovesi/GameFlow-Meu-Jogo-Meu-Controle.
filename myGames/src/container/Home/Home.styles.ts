import * as stylex from '@stylexjs/stylex'; 

export const styles = stylex.create({
  home: {
    maxWidth: '1240px',
    width: '100%',
    height: '100%',
    margin: '0 auto',
    padding: '16px',
    backgroundColor: '#f0f0f0',
  },
});